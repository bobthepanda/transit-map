export interface Dimensions {
    width?: number,
    height?: number,
}

export interface Coordinates {
    x: number,
    y: number,
}

export interface RelativeCoordinates {
    dx: number,
    dy: number,
}